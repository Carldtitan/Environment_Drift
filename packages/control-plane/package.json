{
  "name": "@iwomc/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The IWOMC team control plane: workspaces, devices, contracts, jobs, and audit.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b", "clean": "tsc -b --clean" },
  "dependencies": {
    "@iwomc/contracts": "workspace:*"
  }
}
