{
  "name": "@iwomc/adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IWOMC ecosystem adapter protocol, native adapters, and the honest recognition registry.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": { "build": "tsc -b", "clean": "tsc -b --clean" },
  "dependencies": {
    "@iwomc/contracts": "workspace:*"
  }
}
